# 시크릿 값 예시 파일
# 이 파일을 복사하여 values.secrets.yaml로 사용하세요
# 
# 사용 방법:
# 1. cp values.secrets.yaml.example values.secrets.yaml
# 2. values.secrets.yaml에 실제 시크릿 값 입력
# 3. sops -e values.secrets.yaml > values.secrets.enc.yaml
# 4. values.secrets.yaml은 .gitignore에 의해 Git에 커밋되지 않음

database:
  username: admin
  password: your-secret-password-here

redis:
  password: your-redis-password-here

# 필요한 다른 시크릿들도 여기에 추가
# apiKey: your-api-key-here
# jwtSecret: your-jwt-secret-here

