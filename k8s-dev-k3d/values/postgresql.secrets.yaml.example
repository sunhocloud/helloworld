# PostgreSQL 시크릿 값 예시 파일 (로컬 k3d 환경용)
# 이 파일을 복사하여 postgresql.secrets.yaml로 사용하세요
# 
# 사용 방법:
# 1. cp postgresql.secrets.yaml.example postgresql.secrets.yaml
# 2. postgresql.secrets.yaml에 실제 시크릿 값 입력 (선택사항, 기본값 사용 가능)
# 3. sops -e postgresql.secrets.yaml > postgresql.secrets.enc.yaml
# 4. postgresql.secrets.yaml은 .gitignore에 의해 Git에 커밋되지 않음
#
# 참고: 로컬 개발 환경에서는 간단한 비밀번호를 사용해도 됩니다.

auth:
  username: application
  password: application
  postgresPassword: postgres
  database: groom
  replicationUsername: repl_user
  replicationPassword: repl_password

