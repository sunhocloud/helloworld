# Redis 시크릿 값 예시 파일 (로컬 k3d 환경용)
# 이 파일을 복사하여 redis.secrets.yaml로 사용하세요
# 
# 사용 방법:
# 1. cp redis.secrets.yaml.example redis.secrets.yaml
# 2. redis.secrets.yaml에 실제 시크릿 값 입력 (선택사항, 기본값 사용 가능)
# 3. sops -e redis.secrets.yaml > redis.secrets.enc.yaml
# 4. redis.secrets.yaml은 .gitignore에 의해 Git에 커밋되지 않음
#
# 참고: 로컬 개발 환경에서는 인증을 비활성화하거나 간단한 비밀번호를 사용해도 됩니다.

auth:
  enabled: false  # 로컬에서는 비활성화 가능
  password: ""    # 또는 간단한 비밀번호
  existingSecret: ""

